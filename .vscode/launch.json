{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start Parkerbot",
            "program": "${workspaceRoot}/server/index.js"
        }
    ]
}
