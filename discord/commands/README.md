# Moustacheminer Server Services
## Commands directory
The commands directory is laid out such that each folder is it's own command, and each subdirectory is subcommands, which is dealt with with a different javascript file.